language: php

php:
  - 7.1

install:
  - composer install --no-interaction

before_script:
  - cp .env.travis .env
  - composer self-update

script:
  - php artisan key:generate
  - vendor/bin/phpunit

services:
  - redis-server

cache:
  directories:
    - vendor